
# Overview
This project implements BP (Backpropagation) and CNN (Convolutional Neural Network) based on Python's NumPy foundational functionalities, with MNIST handwritten digit recognition provided as a test case.

# Running
Download the Yuan platform from GitHub repository and install Python. Open the agen_yang directory within the Yuan platform, double-click yang_mnist.py, then click the Run button on the toolbar.
